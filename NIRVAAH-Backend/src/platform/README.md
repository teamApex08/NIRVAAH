# Platform

Technical services used across the application live here: configuration,
database setup, logging, messaging, and observability.

Platform code should support modules without owning business behavior.
