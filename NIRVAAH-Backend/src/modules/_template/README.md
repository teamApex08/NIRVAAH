# Module Template

Copy this directory when adding a new bounded context.

```text
domain/          Business model and rules
application/     Use cases, ports, commands, queries
infrastructure/  Repositories, clients, adapters, persistence
interfaces/      HTTP handlers, event subscribers, CLI adapters
tests/           Module-level tests
```
