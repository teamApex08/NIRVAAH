# Client Data

Frontend-facing sample data, static configuration, and UI fixture data belong
here when they are safe to ship with the client app.

Do not store secrets, private user data, or backend-only datasets in this
folder.
