# Modules

Feature-owned UI lives here. Each module may contain:

- `pages/` for route-level page components.
- `components/` for reusable pieces owned by that module.
- `data/` for frontend fixtures and API-shaped temporary data.
- `hooks/` when stateful behavior belongs only to the module.
