import { NirvaahFooter } from "../shared/NirvaahFooter.jsx";

export function LandingFooter() {
  // The landing page keeps this wrapper so the page reads clearly from top to bottom.
  return <NirvaahFooter id="contact" variant="landing" />;
}
