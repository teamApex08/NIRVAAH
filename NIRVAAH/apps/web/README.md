# NIRVAAH Web

React + Vite landing page for the NIRVAAH product.

The landing page is built with Tailwind utility classes in the React components.
Tailwind CSS is configured through `@tailwindcss/vite`.

## Commands

```bash
npm install
npm run dev
npm run build
```
